let data={name:'Nitin'}
console.log(delete data)  //false

console.log(data)   // after doing above operation still we have data 

// here we try to delete obj directly but  we cannot do that.
// there is no property to delete obj directly.
