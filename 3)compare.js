let a=[20]
let b=['20']

console.log(a[0]==b[0])  //true
console.log(a[0]===b[0]) //false

//here we compare the element index 0 so here we compare element not memory.

//In case 2 we compare type and value both bcz === means comparing both.
